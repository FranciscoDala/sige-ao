services:
    - type: web
    name: sige-frontend
    rootDir: apps/web
    buildCommand: npm install --include=dev && npm run build
    staticPublishPath: dist
    routes:
            - type: rewrite
        source: /*
        destination: /index.html
